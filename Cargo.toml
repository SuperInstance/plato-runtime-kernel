[package]
repository = "https://github.com/SuperInstance/plato-runtime-kernel"
name = "plato-runtime-kernel"
version = "0.1.0"
edition = "2021"
description = "PLATO spatial spreadsheet runtime — room-as-cell tensor bridge, plain-English assertion traps, delta compression, three-way merge"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
